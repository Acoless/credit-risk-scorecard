# Dependencias para correr la app Streamlit (incluye lo que necesita
# el pickle del modelo para deserializarse: optbinning + sklearn).
# IMPORTANTE: pinear versiones exactas. El .pkl se rompe si cambia
# la version de optbinning / scikit-learn / numpy.
streamlit==1.63.0
pandas==3.0.5
numpy==2.5.3
scipy==1.18.1
scikit-learn==1.9.0
optbinning==0.21.0
fpdf2==2.8.8
